

google_api_key <- ""

ui <- fluidPage(
  titlePanel("Melbourne Coffee Shops"),
  sidebarLayout(
    sidebarPanel(
      h3("Search Coffee Shops"),
      textInput("geocode", "Type an address or location", placeholder = "e.g., 123 Collins St"),
      actionButton("go", "Search!"),
      br(),
      helpText("Use the search bar to find coffee shops in Melbourne.")
    ),
    mainPanel(
      leafletOutput("map")
    )
  )
)